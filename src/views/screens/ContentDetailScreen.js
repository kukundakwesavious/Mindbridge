import React from 'react';
import { Share, StyleSheet, Text, View } from 'react-native';
import { colors } from '../../theme/theme';
import { Card, Header, IconBox, PrimaryButton, Screen } from '../components/UI';
export default function ContentDetailScreen({navigation,route}) { const item=route.params.item; const share=()=>Share.share({message:`MindBridge resource: ${item.title}\n${item.summary}`}); return <Screen><Header title="Resource" onBack={()=>navigation.goBack()} right={<Text onPress={share} style={s.share}>Share</Text>}/><Card style={s.hero}><IconBox icon={item.icon} tone={item.tone} size={62}/><Text style={s.title}>{item.title}</Text><Text style={s.meta}>{item.category} • {item.type} • {item.time}</Text></Card>{item.body.map((p,i)=><Text key={i} style={s.paragraph}>{p}</Text>)}<Card style={s.note}><Text style={s.noteTitle}>A gentle reminder</Text><Text style={s.noteText}>This library is for education and support. It does not replace assessment or treatment by a qualified professional.</Text></Card><PrimaryButton title="Talk to a counsellor" icon="chatbubble-ellipses-outline" onPress={()=>navigation.navigate('Counsellors')}/></Screen> }
const s=StyleSheet.create({share:{color:colors.primary,fontWeight:'900'},hero:{alignItems:'flex-start'},title:{fontSize:23,fontWeight:'900',color:colors.text,marginTop:14},meta:{fontSize:11,color:colors.muted,marginTop:5},paragraph:{fontSize:14,lineHeight:23,color:colors.text,marginBottom:15},note:{backgroundColor:'#F1F7FF',borderColor:'#DCEAFF'},noteTitle:{fontWeight:'900',color:colors.text},noteText:{fontSize:12,lineHeight:18,color:colors.muted,marginTop:5}});
